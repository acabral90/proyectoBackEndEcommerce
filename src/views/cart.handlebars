<div>
    <h1>Carrito</h1>
    <br>{{#if arrayProducts.products}}
            {{#each arrayProducts}}
            <div>
                <h2>{{this.title}}</h2>
                <h3>{{this.description}}</h3>
                <h4>{{this.price}}</h4>
            </div>
            {{/each}}
        {{else}}
            <h2>El carrito está vacio</h2>
        {{/if}}
    <br>
</div>

