<div>
    <h1>Carrito</h1>
    <br>
        {{#each arrayProducts}}
        <div>
            <h2>{{this.title}}</h2>
            <h3>{{this.description}}</h3>
            <h4>{{this.price}}</h4>
        </div>
        {{/each}}
    <br>
</div>

